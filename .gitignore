# Python
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# PyInstaller
dist/
build/work/
*.spec

# macOS
.DS_Store

# User data — never commit
config.json
config.json.bak
paperpilot.db
paperpilot.db.bak

# Keys — never commit
.env
*.env

# Eval outputs (contain labeled paper data — raw files excluded)
tests/*.csv
!tests/eval_results_sanitized.csv

# Internal notes
progress_tracking/
